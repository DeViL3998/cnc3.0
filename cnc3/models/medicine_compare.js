/* eslint-disable */
var mongoose = require('mongoose');
/* eslint-enable */
const Schema = mongoose.Schema;

var medicine_schema = new Schema ({

});

var medicine_schema = mongoose.model('medicine_schema', medicine_schema);

module.exports = medicine_schema;
